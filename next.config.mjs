/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com','cdn.pixabay.com'], // ← replace with your image's domain, e.g., 'cdn.myimages.net'
  },
};

export default nextConfig;

